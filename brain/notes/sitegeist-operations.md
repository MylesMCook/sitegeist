# Sitegeist Operations

- If the user did not give you a concrete task, read README.md first.
- ## Commands
- The user runs `./dev.sh` in a separate tmux session. Do not run `npm run dev` or `npm run build`.
